## API Report File for "@bugbubug/llm-kit"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function aggregateStream(stream: AsyncIterable<StreamChunk>, _req?: unknown): Promise<ChatResponse>;

// @public
export interface AiBinding {
    // (undocumented)
    run(model: string, input: Record<string, unknown>, options?: {
        gateway?: AiGatewayOptions;
    }): Promise<unknown>;
}

// @public
export interface AiGatewayOptions {
    // (undocumented)
    cacheTtl?: number;
    collectLogPayload?: boolean;
    // (undocumented)
    id: string;
    // (undocumented)
    skipCache?: boolean;
}

// @public
export function assertGatewayEgress(ctx: ProviderContext, opts?: {
    whitelist?: readonly string[];
}): void;

// @public
export interface ChatMessage {
    // (undocumented)
    parts: Part[];
    // (undocumented)
    role: ChatRole;
}

// @public
export interface ChatModel {
    generate(req: ChatRequest): Promise<ChatResponse>;
    streamChat(req: ChatRequest): AsyncIterable<StreamChunk>;
}

// @public
export interface ChatRequest {
    // (undocumented)
    maxTokens?: number;
    // (undocumented)
    messages: ChatMessage[];
    mockRef?: string;
    // (undocumented)
    model: string;
    // (undocumented)
    purpose?: Extract<Purpose, "chat" | "memory-extract">;
    responseJson?: boolean;
    // (undocumented)
    stream?: boolean;
    system?: string;
    // (undocumented)
    temperature?: number;
    thinking?: ThinkingLevel;
}

// @public
export interface ChatResponse {
    // (undocumented)
    meta?: Record<string, unknown>;
    // (undocumented)
    text: string;
    usage?: Record<string, number>;
}

// @public
export type ChatRole = "user" | "assistant";

// @public (undocumented)
export const cloudflareHooks: ProviderHooks;

// @public (undocumented)
export const createCloudflareProvider: ProviderFactory;

// @public
export function createMockImageModel(options?: {
    resolver?: FixtureResolver;
    latencyMs?: number;
}): ImageModel;

// @public
export function createMockProvider(embeddingDims: number): LlmProvider;

// @public (undocumented)
export function createMockProvider(options: MockOptions): LlmProvider;

// @public
export function createMockVisionModel(options?: {
    resolver?: FixtureResolver;
    latencyMs?: number;
}): VisionModel;

// @public
export function createProviderRegistry(): ProviderRegistry;

// @public
export const DEFAULT_CF_GATEWAY_PREFIXES: readonly ["https://gateway.ai.cloudflare.com/", "https://api.cloudflare.com/"];

// @public
export const defaultHooks: ProviderHooks;

// @public
export interface Embedder {
    // (undocumented)
    embed(req: EmbeddingRequest): Promise<number[][]>;
}

// @public
export interface EmbeddingRequest {
    // (undocumented)
    input: string[];
    // (undocumented)
    model: string;
    // (undocumented)
    purpose?: Extract<Purpose, "memory-embed">;
}

// @public
export function featureHashEmbed(text: string, dims: number): number[];

// @public
export interface FixtureResolver {
    // (undocumented)
    image?(ref: string): ImageResult | undefined;
    // (undocumented)
    text?(ref: string): string | undefined;
    // (undocumented)
    vision?(ref: string): unknown;
}

// @public
export interface ImageModel {
    // (undocumented)
    generate(req: ImageRequest): Promise<ImageResult>;
}

// @public
export interface ImageRequest {
    aspectRatio?: string;
    imageSize?: string;
    mockRef?: string;
    // (undocumented)
    model: string;
    // (undocumented)
    prompt: string;
    refImages?: InlineData[];
}

// @public
export interface ImageResult {
    data: string;
    // (undocumented)
    height?: number;
    // (undocumented)
    mimeType: string;
    usage?: Record<string, number>;
    // (undocumented)
    width?: number;
}

// @public
export interface InlineData {
    data: string;
    // (undocumented)
    mimeType: string;
}

// @public
export function isAllowedGatewayUrl(url: string, whitelist?: readonly string[]): boolean;

// @public
export class LlmKitError extends Error {
    constructor(code: LlmKitErrorCode, message: string,
    options?: {
        cause?: unknown;
    });
    // (undocumented)
    readonly code: LlmKitErrorCode;
}

// @public
export type LlmKitErrorCode = "missing_binding" | "egress_not_allowed" | "dim_mismatch" | "count_mismatch" | "unknown_provider" | "provider_not_configured" | "config_invalid" | "upstream_error" | "response_malformed";

// @public
export interface LlmProvider extends ChatModel, Embedder {
    // (undocumented)
    readonly name: string;
}

// @public
export interface MockOptions {
    // (undocumented)
    embeddingDims: number;
    // (undocumented)
    latencyMs?: number;
    // (undocumented)
    profile?: string;
    // (undocumented)
    resolver?: FixtureResolver;
}

// @public
export function normalizeStream(rawFrames: AsyncIterable<unknown>, hooks: ProviderHooks): AsyncIterable<StreamChunk>;

// @public
export function parseSseFrames(stream: ReadableStream<Uint8Array>): AsyncIterable<unknown>;

// @public
export type Part = {
    text: string;
} | {
    inlineData: InlineData;
};

// @public
export interface ProviderContext {
    ai?: AiBinding;
    // (undocumented)
    chatModel: string;
    // (undocumented)
    embeddingDims: number;
    // (undocumented)
    embedModel: string;
    gatewayId?: string;
    http?: {
        baseUrl: string;
        apiKey: string;
        accountId?: string;
    };
    // (undocumented)
    name: string;
    tokenCache?: TokenCache;
    vertex?: {
        saJson: string;
        projectId: string;
        location: string;
    };
}

// @public
export type ProviderFactory = (ctx: ProviderContext, hooks: ProviderHooks) => LlmProvider;

// @public
export interface ProviderHooks {
    extraChatInput?(req: ChatRequest, ctx: ProviderContext): Record<string, unknown> | undefined;
    normalizeChunk?(rawFrame: unknown): string | undefined;
    retry?: {
        maxAttempts: number;
        backoffMs(attempt: number): number;
        isRetryable(e: unknown): boolean;
    };
    rewriteChat?(req: ChatRequest, ctx: ProviderContext): ChatRequest;
    // (undocumented)
    rewriteEmbed?(req: EmbeddingRequest, ctx: ProviderContext): EmbeddingRequest;
}

// @public
export interface ProviderRegistry {
    // (undocumented)
    create(name: string, ctx: ProviderContext, hooks: ProviderHooks): LlmProvider;
    // (undocumented)
    register(name: string, factory: ProviderFactory): void;
}

// @public
export type Purpose = "chat" | "memory-extract" | "memory-embed";

// @public
export interface StreamChunk {
    // (undocumented)
    error?: string;
    // (undocumented)
    meta?: Record<string, unknown>;
    // (undocumented)
    token?: string;
}

// @public
export type ThinkingLevel = "minimal" | "low" | "medium" | "high";

// @public
export interface TokenCache {
    // (undocumented)
    get(key: string): Promise<string | null>;
    // (undocumented)
    put(key: string, value: string, ttlSeconds: number): Promise<void>;
}

// @public
export interface VisionModel {
    // (undocumented)
    analyze(req: VisionRequest): Promise<VisionResponse>;
}

// @public
export interface VisionRequest {
    image: InlineData;
    mockRef?: string;
    // (undocumented)
    model: string;
    // (undocumented)
    prompt: string;
    responseJson?: boolean;
    thinking?: ThinkingLevel;
}

// @public
export interface VisionResponse {
    // (undocumented)
    analysis: unknown;
    usage?: Record<string, number>;
}

// @public
export function withRetry<T>(fn: () => Promise<T>, hooks: ProviderHooks): Promise<T>;

// (No @packageDocumentation comment for this package)

```
